name: MarkTangBlog

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: Install and Build
        run: |
          npm install
          npm run build

      - name: MarkTangBlog
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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
          publish_dir: ./docs/.vitepress/dist
